import { FinancesStrategy } from './interfaces';

export class PersonalFinancesStrategy implements FinancesStrategy {
  execute(): void {
    //TODO: Step 1 -> Uses datasource with finance data.

    //TODO: Step 2 -> Calcule incomes/expenses representing personal finalces

    //TODO: Step 3 -> Generate output format of report & send it user device
    throw new Error('Method not implemented.');
  }
}
